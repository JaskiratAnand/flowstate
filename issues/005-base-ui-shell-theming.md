## Parent PRD
`docs/PRD.md`

## What to build
Implement the global CSS variables and base UI shell. Build `App.svelte`, `TabBar.svelte`, and `ThemePicker.svelte`. Ensure the layout constraints (380x580) and tab routing work. Implement the 3 built-in themes (Ocean, Forest, Sunset) and Light/Dark modes.

## Acceptance criteria
- [ ] Popup opens at 380px wide and up to 580px tall.
- [ ] Tab bar successfully switches between Timer, Tasks, and Stats views.
- [ ] CSS variables are defined and mapped correctly for all 3 themes.
- [ ] `ThemePicker.svelte` allows switching themes and persists to storage.

## Blocked by
- Blocked by `issues/003-storage-abstraction-layer.md`

## User stories addressed
- User stories 33, 34, 35, 36, 37, 38, 39